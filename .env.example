# ============================================================
# Admin Portal - Root .env
# Copy to .env and fill real values. NEVER commit .env to git.
# ============================================================

# ─── Database ────────────────────────────────────────────────
DB_PASSWORD=SuperSecretDBPass!123

# ─── JWT (generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=replace_with_64_char_random_string_here_now

# ─── CORS (comma-separated, no spaces)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ─── Frontend → Backend URLs (used at BUILD time)
REACT_APP_AUTH_URL=https://yourdomain.com:4001
REACT_APP_COURSE_URL=https://yourdomain.com:4002
REACT_APP_TRAINER_URL=https://yourdomain.com:4003
